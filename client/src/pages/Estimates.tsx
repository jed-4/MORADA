import EstimatesView from "@/components/estimates/EstimatesView";

/** Estimates across every project. Same view as a project's own list. */
export default function Estimates() {
  return <EstimatesView />;
}
